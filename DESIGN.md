# 스파이크 프라임 학과체험 RPG — 기획서 v1.1

> 대상: 중학생 (1~3학년) / 형식: 학과 체험 **부스 (반복 순환형)**
> **1인 총 10분** 안에 부품 이해 → 조립 → 블록코딩 → 해체까지 완주
> 산출물: 단일 HTML 파일(오프라인 실행) RPG 게임 + 게임 내장 매뉴얼

---

## 0. 확정 사항

| 항목 | 결정 | 설계 영향 |
|---|---|---|
| 실물 SPIKE Prime | **있음. 게임은 가이드 역할** | 조립도 일러스트·타이머·확인퀴즈가 핵심. 시뮬레이터 불필요 |
| 플레이 환경 | **개인 PC 1인 1대 (키보드+마우스)** | 방향키 이동 + 마우스 클릭. 가상패드 불필요 |
| 세션 운영 | **반복 순환형 (학생 계속 교체)** | **세이브 없음 / 즉시 시작 / 자동 초기화** 최우선 |
| 아트 톤 | **정통 도트 픽셀 (RPG Maker 2000/VX 풍)** | 384×288 논리 해상도 ×3, 파란 그라데이션 메시지 윈도우, 8bit 사운드 |
| **총 시간** | **10분 올인 (초압축)** | ⚠️ 맵을 작게, 대화를 짧게, 전투는 3문제. 이동 거리 최소화 |
| **조립 대상** | **깃발 흔드는 팔 (약 15조각)** | 중형모터 1개 + 커넥터 + 빔 7칸 + 검정핀. 실패 확률 최저 |
| **실습 화면** | **좌우 분할** | 좌: 쯔꾸르 필드 + 장인 대사 / 우: **조립 가이드 뷰어**(조립도 + 설명 + 주의) |
| **타이머** | **참고용 (강제 진행 없음)** | 남은 시간만 표시. 늦으면 장인 NPC가 힌트로 재촉 |
| **조립·코딩** | **가이드 표시 전용. 게임은 판정하지 않는다** | 체크박스·드래그 퍼즐 없음. 조립도와 정답 코드를 **보여주기만** 한다 → §7-0 · §8-1 |
| **주인공** | **고정 1명 — 견습 메카닉** | 선택 화면 없음. Enter → 즉시 플레이 |
| **엔딩** | **이름 입력 없이 랭크만** | 키보드 입력 0. S/A/B/C 축하 연출 + 도감 수집률 |
| **사운드** | **BGM + 효과음, 기본 재생** | WebAudio 합성. `M` 키 음소거 토글은 유지 |

### 순환형이 강제하는 규칙
1. **타이틀에서 Enter 한 번 → 3초 안에 플레이 시작.** 진입 장벽 금지
2. **이어하기 없음.** 매 판이 완전한 1회 경험
3. **무인 자동 복귀.** 90초 무입력 → 「다음 도전자를 기다립니다」 어트랙트 → 완전 초기화
4. **게임오버 없음.** 틀려도 해설 보고 계속
5. **강사 단축키** 상시 동작

---

## 1. 컨셉

### 1-1. 한 줄 요약
> **"멈춰버린 로봇 왕국 '브릭시티'의 중앙 허브를 되살려라."**
> 도트 그래픽 2D 탑다운 RPG. 부품 정령을 깨우고, 실제 로봇을 조립해 허브를 재가동한다.

### 1-2. 교육 요소 → RPG 장치
| 교육 요소 | RPG 장치 |
|---|---|
| 부품 이름·역할 | **부품 도감** 수집 (= 게임 내장 매뉴얼) |
| 부품 구분 | **퀴즈 배틀** (정답=공격) |
| 조립 순서 | **실물 미션** — 게임은 조립도를 넘겨 보여준다 (판정 없음) |
| 블록코딩 논리 | **주문서(스펠 북) 열람** — 정답 코드를 그대로 제시 |
| 해체·정리 | **보스전** — 문제 1개 정답 = 실제 1단계 해체 |
| 시간 내 완수 | 참고용 타이머 + 랭크 판정 |

### 1-3. 쯔꾸르 문법 채택 목록
- **384×288 논리 해상도**(24×18 타일), 정수배(×3 = 1152×864) 스케일, `image-rendering: pixelated`
- 16×16 타일 그리드 4방향 이동, 캐릭터 3프레임 걷기 (좌-중-우-중)
- 하단 **메시지 윈도우**: 파란 그라데이션 + 흰 이중 테두리, 한 글자씩 타이핑, 깜빡이는 ▼
- NPC 머리 위 `!` 아이콘, 조사키(Z/Space/Enter)로 대화
- 정면 뷰 **턴제 전투** (적 상단, 커맨드 윈도우 하단, 데미지 팝업, 화면 흔들림)
- 맵 전환 페이드 인/아웃, 플래시 연출
- BGM/SE 전부 WebAudio 합성 (외부 파일 0)

---

## 2. 학습 내용 (실제 지식)

### 2-1. 전자 부품 6종 — "6대 정령"
| 게임 내 이름 | 실제 부품 | 학생이 알아야 할 핵심 |
|---|---|---|
| **허브 왕** | Large Hub | 포트 A~F 6개 / 5×5 LED 매트릭스 / 중앙버튼(전원·실행) / 좌우버튼 / 자이로 내장 / 스피커 / 충전식 |
| **강철 거인** | Large Motor | 힘이 센 모터. 바퀴·무거운 구동. 회전각 센서 내장 |
| **쌍둥이 러너** | Medium Motor ×2 | 작고 빠름. 팔·집게·조향 |
| **무지개 눈** | Color Sensor | 색 인식 + 반사광 밝기. 라인트레이싱 |
| **올빼미 눈** | Distance Sensor | 초음파 거리 측정. 눈 부분이 켜지는 LED |
| **주먹 파수꾼** | Force Sensor | 누르는 **세기**까지 측정 |

> 공통 규칙: **모든 부품은 같은 케이블(LPF2)로 허브 포트에 연결.**
> 포트 A~F 아무 데나 꽂아도 되지만, **프로그램에서 지정한 포트와 반드시 일치**해야 한다.

### 2-2. 테크닉 구조 부품 — "연결의 법칙"
| 분류 | 색/모양 | 규칙 |
|---|---|---|
| 마찰 핀 | **검정(2칸)·파랑(3칸)** | 표면이 거칠다 → **고정**용. 안 돌아감 |
| 무마찰 핀 | **연회색·베이지** | 매끈하다 → **회전**이 필요한 곳 |
| 축(액슬) | 십자 단면 | **홀수=회색, 짝수=검정, 2칸만 빨강** |
| 부시 | 회색(1칸)·노랑(½칸) | 축 빠짐 방지 스토퍼 |
| 빔 | 직선/ㄱ자/프레임 | 구멍 개수 = 길이 |
| 커넥터 | 회색 십자블록 | 방향 전환 |

> 🔑 **오개념 교정**: "핀이 안 빠져요" → 색으로 판단. 검정/파랑은 꽉 물리는 게 정상
> 🔑 **분리 도구(브릭 세퍼레이터)**: 손톱·이빨 말고 도구로 지렛대처럼

### 2-3. 블록코딩 — "주문서(Spell Book)"
SPIKE App 워드블록 카테고리를 **색까지 그대로** 재현한다.

| 색 | 카테고리 | 게임 내 호칭 | 대표 블록 |
|---|---|---|---|
| 🟡 노랑 | 이벤트 | 시동 주문 | `프로그램이 시작되면` |
| 🩵 민트 | 모터 | 힘의 주문 | `A 모터를 1회전 시계방향`, `모터 속도 50%` |
| 🔵 파랑 | 움직임 | 이동 주문 | `10cm 직진`, `오른쪽 90도 회전` |
| 🟣 보라 | 조명 | 빛의 주문 | `5×5에 하트 표시` |
| 🩷 분홍 | 소리 | 울림 주문 | `"박수" 소리 재생` |
| 🟠 주황 | 제어 | 시간의 주문 | `2초 기다리기`, `10번 반복`, `만약 ~라면` |
| 🔷 하늘 | 센서 | 감지 주문 | `거리센서가 10cm보다 가까운가?` |
| 🟢 초록 | 연산 | 계산 주문 | `+ - × ÷`, `~보다 큼` |

> 게임 코딩 퍼즐은 **실제 블록과 같은 색·같은 문장**을 쓴다.
> SPIKE App을 켰을 때 "아, 게임에서 본 그거!"가 되는 것이 목적.
> 10분판에서는 🟡 🩵 🟠 3종만 필수, 나머지는 도감 열람용.

---

## 3. 조립 대상 — "첫 번째 골렘 : 깃발 흔드는 팔"

약 15조각. 중형모터 1개만 사용 → 실패 확률 최저.

```
        ▓▓▓   ← 깃발/장식 브릭
         │
      ┌──┴──┐
      │○○○○○│   빔 7칸
      └──┬──┘
       【╳】    십자 커넥터
     ╔═══╨═══╗
     ║  모터  ║  중형 모터
     ╚═══╤═══╝
    ~~~~~│~~~~~ 케이블
   ┌─────┴─────┐
   │ ■■■■■  A  │ 허브 (포트 A)
   └───────────┘
```

### 부품 리스트 (가이드 1페이지에 그대로 사용 → §7-3)
| # | 부품 | 수량 | 게임 내 힌트 |
|---|---|---|---|
| 1 | 허브 | 1 | 「5×5 눈을 가진 왕」 |
| 2 | 중형 모터 | 1 | 「작고 빠른 쌍둥이 중 하나」 |
| 3 | 연결 케이블 | 1 | 「짧은 것으로」 |
| 4 | 빔 7칸 | 1 | 「구멍을 세어봐. 7개」 |
| 5 | 십자 커넥터 | 1 | 「회색, 방향을 꺾는 조각」 |
| 6 | 검정 마찰핀 2칸 | 2 | 「거친 표면 = 고정」 |
| 7 | 장식 브릭 | 1~2 | 「깃발이 될 것」 |

### 목표 코드
```
🟡 프로그램이 시작되면
🟠 4번 반복하기
   🩵 A 모터를 0.3초 동안 시계방향으로 돌리기
   🩵 A 모터를 0.3초 동안 반시계방향으로 돌리기
```
> 블록 **4개**, 카테고리 **3종**(🟡 이벤트 / 🟠 제어 / 🩵 모터).
> 게임은 이 코드를 **그대로 보여준다.** 학생은 SPIKE 앱에 옮겨 적는다 → **§8**

### 해체 규칙 (보스전 문제로 그대로 출제)
1. **케이블 먼저** (선 말고 커넥터를 잡고 뽑기)
2. 장식 → 빔 → 커넥터 → 모터 (**조립 역순**)
3. 핀은 **분리 도구**로
4. 부품별 **칸에 분류**해서 정리

---

## 4. 게임 구조 (10분 올인)

### 4-1. 전체 타임라인

| 구간 | 시간 | 화면 | 내용 |
|---|---|---|---|
| 어트랙트 | — | 데모 루프 | 「Press Enter」 |
| **오프닝** | 0:00~0:20 | 컷신 | 20초 스토리. Enter로 스킵 |
| **MAP 1 — 허브 마을** | 0:20~2:00 | 필드 | 도감 할머니(부품 6종 카드) → 대장장이(핀 색 규칙) → 문지기 **퀴즈 배틀 3문제** |
| **MAP 2 — 조립 공방** | 2:00~7:30 | **좌우 분할** | 조립 가이드 6페이지 (부품찾기 → 모터 → 빔 → 깃발 → 케이블+확인퀴즈 2문제 → 주문서). 상세 §7-2 |
| **MAP 3 — 해체의 방 (보스)** | 7:30~9:20 | 전투+분할 | 보스「혼선 케이블」. **정답 1개 = 실제 1단계 해체** ×4 |
| **엔딩** | 9:20~10:00 | 수료증 | 랭크 + 도감 수집률 + 이름 |

> MAP 1의 필드 이동 거리는 **한 화면 안**으로 제한 (좌→우 일직선 배치).
> 길 찾다가 시간 까먹는 일이 없게 한다.

### 4-2. MAP 2 — 좌우 분할 화면 (핵심 화면)

> 아래는 초기 스케치다. **확정 규격은 §7-1**을 따른다 (체크리스트 → 설명·주의 영역으로 교체).

```
┌──────────────┬────────────────────┐
│ ░░ 조립 공방 ░░│  STEP 2 / 6  조립   │
│              │  ┌──────────────┐  │
│    [@]       │  │   ▓▓▓        │  │
│   [!장인]     │  │  【모터】      │  │  ← 도트 조립도
│  ■ 작업대 ■   │  └──────────────┘  │
│              │  ☑ 중형 모터 1개    │
├──────────────┤  ☑ 빔 7칸          │
│ 로봇 장인     │  ☐ 십자 커넥터 1개  │
│「빔 7칸을     │  ☐ 검정 마찰핀 2개  │
│  커넥터에…」▼│   ⏱ 7:32   [완료 ▶]│
└──────────────┴────────────────────┘
```
- 좌측 필드는 계속 살아있음 (플레이어 이동 가능, 장인에게 말 걸면 현재 페이지 대사 반복)
- 우측 패널은 페이지마다 내용 교체 (`◀ 이전` / `다음 ▶`)
- 타이머는 **참고용**. 예정 시간 초과 시 장인이 「서두르자!」 대사 + 숫자 노랑→빨강

### 4-3. 전투 시스템 — "퀴즈 배틀"
- 정면 뷰. 적 도트 스프라이트 + 하단 커맨드 윈도우
- 4지선다 / OX / 부품 그림 고르기
- **정답** → 공격 (데미지 팝업 + 화면 흔들림 + 8bit 타격음)
- **오답** → 적 공격 + **해설 창** (틀려도 배우고 감)
- **HP 0이어도 게임오버 없음** → 「정령이 힘을 빌려준다!」 회복 후 재도전

#### 문제 풀 (10분판 최소 구성)
| 위치 | 문제 수 | 주제 |
|---|---|---|
| 문지기 (MAP1) | 3 | 부품 이름·역할, 핀 색 규칙 |
| 조립 중 확인 (MAP2) | 2 | 포트 일치, 케이블 연결 — 케이블 페이지를 넘길 때 `battle` 씬 재사용 |
| 보스 (MAP3) | 4 | 해체 순서 (= 실제 해체 단계) |

### 4-4. 보상
- **부품 도감**: 만난 부품 자동 등록 + 상세 스펙 (게임 내장 매뉴얼 본체)
- **랭크**: 정답률 + 소요시간 → S / A / B / C
- **수료증**: 이름 + 랭크 + 날짜 + 도감 수집률

### 4-5. 강사 운영 기능
| 키 | 기능 |
|---|---|
| `Ctrl` + `R` | 즉시 초기화 → 어트랙트 (다음 학생) |
| `F1` | 조작법 오버레이 |
| `F2` | 챕터 점프 (MAP 1/2/3) |
| `F3` | 타이머 일시정지 |
| 90초 무입력 | 자동 초기화 (10초 경고 후) |

---

## 5. 기술 설계

| 항목 | 방침 | 이유 |
|---|---|---|
| 배포 | **단일 .html 파일** (JS·CSS·에셋 인라인) | 학교 PC 더블클릭 실행, 인터넷 불필요 |
| 논리 해상도 | **384×288** (24×18 타일), 정수배 스케일(×2~×4 자동) | 4:3 유지 + 맵 1장 = 정확히 한 화면. 좌우 분할 시 192+192로 딱 떨어진다 |
| 렌더링 | Canvas 2D (`imageSmoothingEnabled=false`) **단독** | 드래그 UI를 폐기(§8-1)해 DOM 오버레이가 필요 없어졌다. 도트 일관성 유지 |
| 그래픽 | **문자열 픽셀맵 → 런타임 스프라이트 생성** | 외부 이미지 0, 도트 직접 제어 |
| 한글 폰트 | **안티에일리어싱 제거 기법** — 시스템 폰트로 문자열을 오프스크린에 그린 뒤 알파를 이진화(α>128→255)해서 비트맵 캐시 | 폰트 파일 임베드 없이 진짜 도트 한글. 라이선스·용량 문제 0 |
| 사운드 | WebAudio 합성 (구형파/삼각파 8bit) | 파일 없이 BGM·SE |
| 저장 | **없음** | 순환형 |
| 입력 | 방향키/WASD, Z·Space·Enter 확인, X·Esc 취소, 마우스 클릭 | 개인 PC 전제. **전 UI에 키보드 폴백**(§7-4) |

### 디렉터리 (예정)
```
spike-prime-rpg/
├─ DESIGN.md
├─ index.dev.html              개발용 (개별 <script> 로드)
├─ src/
│  ├─ core/    const.js engine.js input.js renderer.js font.js audio.js
│  │           sprites.js sprites_parts.js tiles.js state.js hotkeys.js idle.js
│  ├─ scenes/  attract.js field.js battle.js cards.js dex.js ending.js
│  │           └ (S5 추가 예정) mission.js
│  ├─ data/    parts.js quiz.js maps.js dialogue.js bgm.js
│  │           └ (S5·S6 추가 예정) steps.js  blocks.js
│  └─ ui/      messageWindow.js
│              └ (S5·S6 추가 예정) missionPanel.js
├─ scripts/smoke.mjs           헤드리스 스모크 테스트
├─ build.mjs                   단일 HTML 병합
└─ dist/spike-prime-rpg.html   최종 배포본
```

> 가이드 패널(`missionPanel.js`)은 **씬이 아니라 필드 위에 얹히는 UI 모듈**이다.
> 코드 가이드는 이 패널의 마지막 페이지이므로 **별도 파일이 없다.**
> `field.js`가 `map.split === true`일 때 좌측 192px만 클리핑해 그리고, 우측 192px를 패널에 넘긴다 (이미 구현됨).

---

### 코드 방침
- **ES 모듈 대신 전역 네임스페이스 `SPIKE`** + 클래식 `<script>` — `file://`에서 CORS 없이 바로 열림
- `index.dev.html` : 개별 스크립트 로드 (개발·디버깅용)
- `npm run build` → `build.mjs` → `dist/spike-rpg.html` (강사 배포본)

---

## 6. 개발 순서

| 단계 | 상태 | 내용 | 산출물 | 상세 |
|---|---|---|---|---|
| **S1** | ✅ | 코어 — 루프·입력·384×288 렌더러·도트폰트·오디오 | 빈 화면에 도트 한글 출력 | §5 |
| **S2** | ✅ | 필드 — 타일맵·캐릭터 이동·충돌·NPC·메시지 윈도우 | MAP1 걸어다니며 대화 | §4 |
| **S3** | ✅ | 데이터 — 부품 도감·퀴즈 풀·대사 | 도감 열람 가능 | §2 |
| **S4** | ✅ | 전투 — 퀴즈 배틀 + 해설 | 문지기 전투 동작 | §4-3 |
| **S5** | ✅ | 가이드 뷰어 — 좌우 분할 패널·조립도 6장·페이지 넘김 | MAP2 완주 | **§7** |
| **S6** | ✅ | 코드 가이드 — 주문서 카드 + 실행 순서 애니메이션 | 정답 코드 표시 | **§8** |
| **S7** | ✅ | 보스·엔딩·어트랙트·강사 단축키 | 10분 풀 플레이 | §9 · §10 |
| **S8** | ⬜ | 폴리싱 — BGM·연출·밸런스·단일 파일 빌드 | `dist/spike-prime-rpg.html` | §11 · §13 |

---

## 7. MAP 2 — 조립 가이드 화면 (S5)

### 7-0. 방침 — **게임은 판정하지 않는다. 보여줄 뿐이다.**

실물 조립의 진위는 게임이 알 수 없고, 알 필요도 없다.
조립이 제대로 됐는지는 **옆에 있는 강사와 학생 본인의 눈**이 판정한다.

따라서 우측 패널은 **읽기 전용 가이드 뷰어**다.

| 넣지 않는 것 | 이유 |
|---|---|
| 체크박스 / 체크 판정 | 실물을 안 만지고 체크만 눌러도 통과된다 = 무의미한 관문 |
| 조립 완료 판정 | 게임이 확인할 방법이 없다 |
| 드래그 앤 드롭 코딩 퍼즐 | §8로 대체. **정답 코드를 그대로 보여준다** |
| 스텝별 강제 타이머 | 참고용 표시만 유지 |

| 대신 넣는 것 | 이유 |
|---|---|
| 큼직한 **도트 조립도** | 이 화면의 본체. 그림 하나가 설명 세 줄을 이긴다 |
| 짧은 **설명 2~3줄** | 읽는 데 5초를 넘기지 않는다 |
| **⚠ 주의 한 줄** | 그 단계에서 제일 많이 하는 실수 |
| `◀ 이전` / `다음 ▶` | 유일한 조작. 되돌아볼 수 있어야 한다 |

> 이 결정으로 S5·S6의 구현량이 **파일 5개 → 3개**로 줄고,
> 남은 작업의 대부분이 "조립도 도트를 예쁘게 그리는 일"이 된다.

### 7-1. 화면 분할 규격

```
 0                        192                        384
 ┌─────────────────────────┬─────────────────────────┐ 0
 │                         │  ■ 헤더 (22px)          │
 │   필드 뷰포트            │  3 / 6  빔 고정  ●●●○○○  │
 │   192 × 288             ├─────────────────────────┤ 22
 │   (field.js가 clip)      │                         │
 │                         │  ■ 조립도 (140px)        │
 │        [@]              │    도 트  일 러 스 트     │
 │      [!장인]             │    + 화살표 · 강조 점멸   │
 │                         │                         │
 │                         ├─────────────────────────┤ 162
 ├─────────────────────────┤  ■ 설명 (82px)          │
 │  메시지 윈도우           │   본문 2~3줄            │
 │  x=4 w=184 h=66         │   ⚠ 주의 1줄            │
 │  (필드 쪽에만 뜬다)      ├─────────────────────────┤ 244
 │                         │ ◀ 이전  ⏱7:32  다음 ▶  │ 44px
 └─────────────────────────┴─────────────────────────┘ 288
```

| 영역 | 좌표(논리px) | 내용 |
|---|---|---|
| 필드 뷰포트 | `0,0 192×288` | `field.js` 기존 clip 그대로 |
| 헤더 | `192,0 192×22` | `n / 6` + 페이지 제목 + 진행 점 `●●●○○○` |
| **조립도** | `192,22 192×140` | 화면에서 가장 큰 영역. 부품 스프라이트 조합 + 흰 화살표 |
| 설명 | `192,162 192×82` | 본문 10px 도트 한글 2~3줄 + `⚠` 주의 1줄(`textHi`) |
| 푸터 | `192,244 192×44` | `◀ 이전` · 타이머 · `다음 ▶` |

- 패널 배경은 메시지 윈도우와 **같은 스킨**(`winTop`→`winBot` + 흰 이중 테두리).
- 좌우 경계에 세로 1px `winEdge2`.
- 카메라 고정(공방 맵은 좌측 12열만 바닥). 플레이어는 12×18 안에서만 이동.

### 7-2. 가이드 페이지 — 6장 / 5분 30초

§4-1의 러프한 배분을 다음으로 대체한다.

| # | id | 제목 | 예산 | 누적 |
|---|---|---|---|---|
| 1 | `find` | 부품 찾기 | 0:40 | 2:40 |
| 2 | `motor` | 모터 + 커넥터 | 0:50 | 3:30 |
| 3 | `beam` | 빔 고정 | 1:00 | 4:30 |
| 4 | `flag` | 깃발 달기 | 0:40 | 5:10 |
| 5 | `cable` | 케이블 · 포트 A | 0:40 | 5:50 |
| 6 | `code` | 주문서 (코드 가이드) | 1:40 | 7:30 |

> 예산은 **페이스메이커 표시일 뿐** 강제하지 않는다.
> 페이지 예산을 20초 넘기면 좌측 장인이 재촉 대사 1회를 띄운다. 그게 전부다.

### 7-3. 페이지별 내용

각 페이지 = `{ 제목 / 조립도 / 본문 2~3줄 / ⚠주의 / 장인 대사 }`

---

#### 1 — 부품 찾기 `find`
- **조립도**: 부품 7종 아이콘 2열 나열 + 각 아이콘 아래 수량
- **본문**
  ```
  트레이에서 일곱 가지를 꺼내자.
  빔은 구멍을 세어라 — 7개짜리다.
  ```
- **⚠ 주의**: `모터는 작은 쪽. 큰 것은 라지 모터다`
- **장인**: 「필요한 건 일곱 가지다.\n오른쪽 그림과 같은 걸 꺼내라.」

| 부품 | 수량 | 구분 요령 |
|---|---|---|
| 허브 | 1 | 5×5 눈을 가진 것 |
| 중형 모터 | 1 | 작은 쪽 |
| 연결 케이블 | 1 | 가장 짧은 것 |
| 빔 7칸 | 1 | 구멍 7개 |
| 십자 커넥터 | 1 | 회색, 방향 꺾는 조각 |
| 검정 마찰핀 | 2 | 거친 표면 |
| 장식 브릭 | 1~2 | 깃발이 될 것 |

---

#### 2 — 모터 + 커넥터 `motor`
- **조립도**: 모터 위에서 커넥터가 내려오는 ↓ 화살표 (0.8초 루프). 모터의 십자 축을 노랑으로 강조
- **본문**
  ```
  모터 가운데 튀어나온 십자 축에
  커넥터의 십자 구멍을 끼운다.
  끝까지 눌러 넣는다.
  ```
- **⚠ 주의**: `안 들어가면 힘이 아니라 방향. 90도 돌려보자`
- **장인**: 「구멍이 두 종류다 — 십자와 동그라미.\n축은 십자에만 들어간다.」
- **개념**: 십자 구멍 = 축용 / 동그란 구멍 = 핀용

---

#### 3 — 빔 고정 `beam`
- **조립도**: 커넥터 위에 빔이 얹히고 핀 2개가 → ← 로 박힘. 핀 2곳을 빨강 원으로 강조
- **본문**
  ```
  빔 7칸의 끝 구멍을 커넥터에 맞추고
  검정 핀 두 개로 고정한다.
  ```
- **⚠ 주의**: `핀이 하나면 관절이 되어 덜렁거린다. 반드시 두 개`
- **장인**: 「검정은 거칠거칠 — 안 돌아간다.\n잘 안 들어가는 게 정상이다.」
- **개념(오개념 교정)**: 핀 1개 = 회전축 / **핀 2개 = 고정**

---

#### 4 — 깃발 달기 `flag`
- **조립도**: 완성 형태 + 깃발 위치 점멸
- **본문**
  ```
  빔의 반대쪽 끝, 모터에서 가장 먼 곳에
  장식 브릭을 붙인다.
  ```
- **⚠ 주의**: `가볍게. 무거우면 모터가 힘들어한다`
- **장인**: 「모양은 자유다. 네 골렘이니까.」
- **연출**: 이 페이지를 넘길 때 `SE: fanfare_s` + 「골렘의 몸이 완성되었다!」 토스트 1.2초

---

#### 5 — 케이블 · 포트 A `cable`
- **조립도**: 모터 ─ 케이블 ─ 허브. **A 포트 칸이 노랑 점멸**
- **본문**
  ```
  케이블 한쪽을 모터에,
  반대쪽을 허브의 A 포트에 꽂는다.
  딸깍 소리가 나야 끝까지 들어간 것.
  ```
- **⚠ 주의**: `프로그램이 A라고 하면 반드시 A. 오늘 제일 많이 틀리는 곳`
- **장인**: 「몸은 됐다. 이제 생명선이다.」
- **확인 퀴즈 2문제**: 이 페이지를 넘길 때 `S.QUIZ.mission`을 **기존 `battle` 씬 그대로** 호출 (적 `e_cable` / 이름 「꼬인 케이블」). 전투 시스템은 S4에서 이미 완성돼 있어 **추가 구현 0**
  - 오답이어도 진행. 해설만 보여주고 넘어간다
  - 이 2문제가 MAP2의 유일한 판정이다 — 조립이 아니라 **이해**를 묻기 때문에 성립한다

---

#### 6 — 주문서 `code`
- 패널이 **코드 가이드 카드**로 교체된다 → **§8**
- **장인**: 「마지막이다. 오른쪽 주문서를 보고\nSPIKE 앱에 똑같이 옮겨 적어라.」
- 마지막 페이지의 버튼은 `다음 ▶` 대신 **`완료 ▣`**
  → `flags.missionDone = true` → 북쪽 문 열림 → MAP 3

### 7-4. 페이지 넘김 UX

| 조작 | 동작 |
|---|---|
| `다음 ▶` 클릭 · `Enter` · `Z` · `Space` | 다음 페이지 |
| `◀ 이전` 클릭 · `Backspace` | 이전 페이지 (**1페이지에서는 비활성**) |
| `←` `→` | 이전 / 다음 (좌측 필드 이동과 충돌하지 않게 **좌우만** 패널에 할당) |
| 방향키 `↑` `↓` / `WASD` | 좌측 필드 플레이어 이동 (그대로 유지) |

- **되돌아가기를 반드시 열어둔다.** 놓친 학생이 앞 단계를 다시 볼 수 없으면 가이드가 아니다.
- 페이지 전환은 0.15초 슬라이드 인. 그 이상은 시간 낭비.
- 좌측 장인에게 말 걸면 **현재 페이지의 장인 대사**를 다시 들려준다 (대화 중에는 패널 클릭 무시).

### 7-5. 조립도 그리기 규격

이 프로젝트에서 **가장 공들일 부분**이다. 남은 작업의 절반이 여기다.

| 항목 | 규격 |
|---|---|
| 캔버스 | `192 × 140`, 내부 여백 8px |
| 부품 | `sprites_parts.js`의 기존 도트를 **2배 확대**해서 조합 (`p_motor_m`, `p_beam`, `p_connector`, `p_pin_black`, `p_cable`, `p_hub`, `p_flag`) |
| 화살표 | 흰색 3px 굵기 + 검정 1px 외곽. 이동 방향으로 0.8초 루프 왕복 |
| 강조 | 대상 부위를 `textHi` 원/사각으로 감싸고 0.5초 주기 점멸 |
| 라벨 | 부품 옆 8px 도트 한글 (`모터`, `빔 7칸` …) |
| 배경 | 반투명 검정 `#00000033` 패널. 부품 실루엣이 묻히지 않게 |

```js
// missionPanel.js — 데이터가 아니라 렌더 함수로 관리
var DIAGRAMS = {
  d_parts:  function (r, x, y, t) { /* 부품 7종 2열 */ },
  d_motor:  function (r, x, y, t) { /* 모터 + 커넥터 ↓ */ },
  d_beam:   function (r, x, y, t) { /* 빔 + 핀 2개 → ← */ },
  d_flag:   function (r, x, y, t) { /* 완성체 + 깃발 점멸 */ },
  d_cable:  function (r, x, y, t) { /* 모터-케이블-허브 A점멸 */ },
  d_code:   function (r, x, y, t) { /* §8 주문서 카드 */ }
};
```

`t` = 페이지 진입 후 경과 초. 애니메이션은 전부 `t`만 보고 그린다 (상태 없음 → 리셋 공짜).

### 7-6. 데이터 스키마 — `src/data/steps.js`

```js
S.GUIDE_PAGES = [
  {
    id: 'find',
    title: '부품 찾기',
    goal: '트레이에서 부품 7종을 꺼내자',   // 좌측 상단 퀘스트 문구
    budget: 40,                            // 초 (표시용)
    diagram: 'd_parts',
    body: [
      '트레이에서 일곱 가지를 꺼내자.',
      '빔은 구멍을 세어라 — 7개짜리다.'
    ],
    warn: '모터는 작은 쪽. 큰 것은 라지 모터다',
    maker: '필요한 건 일곱 가지다.\n오른쪽 그림과 같은 걸 꺼내라.',
    late:  '부품 찾기부터 오래 걸리면 뒤가 빡빡해진다.',
    learn: ['hub','motorM','cable','beam','connector','pinBlack','flag'],
    onLeave: null        // 'quiz:mission' | null
  }
  // ... 6장
];
```

| 필드 | 용도 |
|---|---|
| `diagram` | `DIAGRAMS`의 키. 데이터와 렌더를 분리 |
| `body` / `warn` | 설명 영역. `body`는 최대 3줄, `warn`은 1줄 |
| `maker` | 장인에게 말 걸었을 때 대사 |
| `late` | 예산 초과 시 1회 |
| `learn` | 페이지를 **지나갈 때** 도감 자동 등록 |
| `onLeave` | 페이지를 넘길 때 실행할 특수 처리 (`cable` 페이지만 `'quiz:mission'`) |

### 7-7. 모듈 — 3개 파일

| 파일 | 책임 | 규모(예상) |
|---|---|---|
| `data/steps.js` | `S.GUIDE_PAGES` 데이터만 | ~120줄 |
| `scenes/mission.js` | `S.Mission` — 페이지 인덱스 관리. `reset() / talk(fd) / next() / prev() / isDone()` | ~90줄 |
| `ui/missionPanel.js` | `S.MissionPanel` — 패널 렌더 + `DIAGRAMS` + 클릭/키 처리 | ~300줄 (대부분 조립도) |

> `state.js`는 이미 `S.Mission.reset()`을, `field.js`는 `S.MissionPanel.enter/exit/draw`를 호출한다.
> **이 3개만 추가하면 배선 끝.** (`index.dev.html`·`build.mjs` 로드 순서 등록 필요)
> `ui/dragBoard.js`는 **만들지 않는다.**

---

## 8. 코드 가이드 (S6)

### 8-1. 방침 — **퍼즐이 아니라 악보다**

드래그 앤 드롭 퍼즐을 **폐기한다.** 대신 **완성된 코드를 그대로 보여준다.**

| 폐기 이유 | |
|---|---|
| 시간 | 1:40 안에 퍼즐을 풀고 **다시 SPIKE 앱에서 똑같이 만들면** 두 번 일한다 |
| 목적 | 오늘의 목표는 "블록을 읽고 앱에서 찾을 수 있다"이지 "퍼즐을 푼다"가 아니다 |
| 구현 | 드래그·스냅·판정·오답해설 = 가장 비싼 기능인데 학습 효과는 가장 얇다 |

학생이 할 일: **오른쪽 그림을 보고 SPIKE 앱에 똑같이 옮긴다.**
게임이 할 일: **어느 서랍에서 어떤 블록을 꺼내는지 정확히 보여준다.**

### 8-2. 주문서 카드

```
┌─────────────────────────┐
│ 6 / 6  주문서     ●●●●●●│
├─────────────────────────┤
│  【 주문서 】             │
│ ┌─────────────────────┐ │
│ │ 🟡 프로그램이 시작되면│ │  ← 이벤트(노랑)
│ │ 🟠 4번 반복하기      │ │  ← 제어(주황)
│ │ 🩵   A모터 0.3초 ↻  │ │  ← 모터(민트) 들여쓰기
│ │ 🩵   A모터 0.3초 ↺  │ │  ← 모터(민트) 들여쓰기
│ └─────────────────────┘ │
│                         │
│  SPIKE 앱에서 찾는 법    │
│  🟡 노랑 서랍 → 맨 위    │
│  🟠 주황 서랍 → 반복하기 │
│  🩵 민트 서랍 → 모터 시간│
├─────────────────────────┤
│ ◀ 이전  ⏱ 4:10  완료 ▣ │
└─────────────────────────┘
```

### 8-3. 블록 렌더 규격

실제 SPIKE App과 **같은 색, 같은 문장**을 쓴다. 앱을 켰을 때 "아, 게임에서 본 그거!"가 되는 것이 전부다.

| 항목 | 규격 |
|---|---|
| 블록 크기 | `168 × 20` px (들여쓰기 블록은 `152 × 20`, x+16) |
| 배경 | `SPIKE.BLOCK_CATS`의 카테고리 색 (`const.js` 기정의) |
| 외곽 | 검정 1px + 상단 1px 밝은선(입체감) |
| 좌측 홈/돌기 | SPIKE 워드블록의 퍼즐 모양을 2px 도트로 흉내 |
| 텍스트 | 10px 도트 한글, 흰색. 어두운 배경엔 흰색, 노랑 배경엔 검정 |
| 반복 블록 | 아래·왼쪽으로 감싸는 `ㄴ`자 테두리 → **"안에 들어 있다"가 눈에 보이게** |

### 8-4. 실행 순서 애니메이션

읽기를 돕는 **유일한** 연출. 3초 루프.

1. 🟡 블록 0.4초 하이라이트 (흰 테두리)
2. 🟠 블록 0.4초 하이라이트 + 우측에 `1회차` 카운터
3. 🩵 두 줄이 번갈아 0.25초씩 × 4회 반복 → 카운터 `1→2→3→4`
4. 조립도 썸네일(우측 상단 48×48)의 깃발이 좌우로 흔들림
5. 0.6초 정지 후 처음부터

> 이 3초짜리 루프 하나가 "반복 4번 = 팔이 4번 흔들린다"를 말없이 설명한다.
> **설명 문장을 하나 줄일 수 있으면 애니메이션이 이긴다.**

### 8-5. SPIKE 앱 대응표 (카드 하단)

가이드이므로 **학생이 앱에서 블록을 찾을 수 있어야 한다.**

| 블록 | 서랍(카테고리) | 앱에서의 위치 |
|---|---|---|
| `프로그램이 시작되면` | 🟡 이벤트 | 서랍 맨 위 첫 블록 |
| `4번 반복하기` | 🟠 제어 | 두 번째 블록. 숫자 칸에 `4` 입력 |
| `A 모터 0.3초 시계방향` | 🩵 모터 | 「_초 동안 _방향으로」 블록. 포트 `A`, 시간 `0.3` |
| `A 모터 0.3초 반시계방향` | 🩵 모터 | 같은 블록에서 **방향 화살표만 반대로** |

- 패널 공간상 4줄이 전부 안 들어가면 **3초마다 2줄씩 교대 표시**한다.
- 포트가 `A`인 이유는 5페이지에서 이미 꽂았기 때문 — 카드에 `포트 A` 배지를 노랑으로 강조.

### 8-6. 데이터 스키마 — `src/data/blocks.js`

```js
/* 보여주기 전용. 판정도 팔레트도 없다. */
S.CODE_GUIDE = {
  title: '깃발 흔드는 주문',
  lines: [
    { cat: 'event', indent: 0, text: '프로그램이 시작되면' },
    { cat: 'ctrl',  indent: 0, text: '4번 반복하기', wrap: true },  // wrap: ㄴ자 테두리
    { cat: 'motor', indent: 1, text: 'A 모터 0.3초 ↻' },
    { cat: 'motor', indent: 1, text: 'A 모터 0.3초 ↺' }
  ],
  findIt: [
    { cat: 'event', text: '노랑 서랍 → 맨 위' },
    { cat: 'ctrl',  text: '주황 서랍 → 반복하기' },
    { cat: 'motor', text: '민트 서랍 → _초 동안 _방향' }
  ]
};
```

색은 `SPIKE.BLOCK_CATS`에서 `cat` 키로 조회 (`const.js`에 이미 정의됨).

### 8-7. 모듈

| 파일 | 책임 |
|---|---|
| `data/blocks.js` | `S.CODE_GUIDE` 데이터만 (~40줄) |
| `ui/missionPanel.js` | `DIAGRAMS.d_code`가 주문서 카드를 그린다. **별도 파일 없음** |

> `ui/dragBoard.js`는 **폐기.** `S.BLOCKS` / `S.CODE_PUZZLE` 스키마도 폐기.

### 8-8. 도감과의 연결 (남는 여유의 사용처)

드래그 퍼즐을 뺀 만큼, 도감(`X` 키)에 **블록 카테고리 8종 페이지**를 추가한다.

- 색 + 게임 내 호칭 + 대표 블록 1개 (§2-3 표 그대로)
- 오늘 안 쓰는 🔵🟣🩷🔷🟢 5종도 열람 가능 → **"다음에 배울 것"을 보여주는 후킹**
- 구현: 기존 `scenes/dex.js`에 탭 하나 추가. 신규 파일 0

---
## 9. MAP 3 — 보스전과 실물 해체의 결합

문제 4개는 `S.QUIZ.boss`에 이미 작성되어 있다. 여기서는 **연출 규약**을 확정한다.

### 9-1. 진행 루프

```
문제 n 출제
  → 정답:  적에게 타격 + 화면 흔들림
           + ★ 해체 지시 카드 (act) 전체화면 1.5초
           + 「지금 실제로 해보자」 대기 — Z 누를 때까지 다음 문제 없음
  → 오답:  적 반격 (HP -1) + 해설 창 → 같은 문제 재출제 (보기 순서만 섞음)
```

- **오답 시 다음으로 넘어가지 않는다.** 보스전 문제는 곧 실물 해체 절차이므로, 순서를 건너뛰면 실물이 어긋난다. (MAP1 퀴즈와의 차이점)
- `act` 카드는 큰 도트 글씨 + 해당 부품 아이콘. 예: `① 케이블을 뽑는다` + `p_cable`
- 4문제 = 4단계 해체. 마지막 카드는 `④ 부품을 칸에 분류해 정리한다`

### 9-2. 보스 「혼선 케이블」

| 항목 | 값 |
|---|---|
| 스프라이트 | `e_cable` (뒤엉킨 케이블 덩어리, 눈 2개) |
| HP | 4 (문제 수와 동일 — 정답 1 = 데미지 1) |
| 피격 | 엉킨 가닥이 한 줄씩 풀리는 4단계 스프라이트 변형 |
| 격파 | 완전히 풀려 바닥에 늘어짐 + 화면 플래시 + `SE: boss_down` |
| 플레이어 HP 0 | 게임오버 없음 → 「정령이 힘을 빌려준다!」 HP 전회복 + 같은 문제 계속 |

### 9-3. 격파 후

1. `flags.bossDone = true`
2. 허브 제단으로 이동해 조사 → 허브 왕 각성 대사 (`dialogue.js`에 이미 작성됨)
3. → `ending` 씬

---

## 10. 엔딩 · 랭크 · 수료증

### 10-1. 랭크 산정

`state.js`의 현재 공식을 확정안으로 채택하고, 해체 완주 조건을 추가한다.

```
정답률 acc = quizOk / (quizOk + quizNg)      ※ 코딩 퍼즐 시행착오는 제외
시간내   onTime = elapsed <= 600초

S : acc >= 0.90  AND  onTime  AND  bossDone
A : acc >= 0.75
B : acc >= 0.50
C : 그 외
```

- **C도 실패가 아니다.** 「완주 인증」으로 표기한다. 부스에서 기죽는 학생이 나오면 안 된다.
- 전체 문제 수는 9문제(3+2+4)뿐이라 1문제 차이가 랭크를 가른다 — 의도된 것(다시 도전하고 싶게).

### 10-2. 수료증 화면

```
┌──────────────────────────────────┐
│   ☆ 수 료 증 ☆                   │
│                                  │
│      브릭시티 견습 메카닉          │
│                                  │
│        R A N K :  A              │  ← 큰 도트, 랭크색
│                                  │
│  퀴즈 정답    7 / 9              │
│  소요 시간    9:12               │
│  도감 수집    15 / 15  (100%)     │
│                                  │
│  ─────────────────────────       │
│  "첫 번째 골렘을 깨우고            │
│   허브 왕을 되살렸다."             │
│                                  │
│      Enter — 다음 도전자에게       │
└──────────────────────────────────┘
```

- **이름 입력 없음**(§0 확정). 키보드 입력 0.
- 랭크 색: S `#ffe066` / A `#7dff9b` / B `#54c7ec` / C `#ffffff`
- S랭크는 반짝이 파티클 + 팡파레 롱버전. **A 이하도 반드시 팡파레가 있다.**
- Enter 또는 20초 경과 → 어트랙트로 복귀 + `State.reset()`

---

## 11. 사운드 설계

전부 WebAudio 합성. `data/bgm.js`에 트랙, `core/audio.js`에 SE.

| 키 | 용도 | 성격 |
|---|---|---|
| `title` | 어트랙트 | 느린 아르페지오, 4마디 루프 |
| `village` | MAP1 | 밝은 장조, 삼각파 멜로디 + 구형파 베이스 |
| `workshop` | MAP2 | 작업감. 8분음표 반복, 멜로디 절제 (**집중 방해 금지**) |
| `boss` | MAP3 | 단조, 빠른 베이스 |
| `ending` | 수료증 | 팡파레 → 잔잔한 루프 |

| SE | 상황 |
|---|---|
| `cursor` / `decide` / `cancel` | 메뉴 |
| `talk` | 메시지 타이핑 (2글자마다 1회, 짧게) |
| `page` | 가이드 페이지 넘김 (짧은 종이 넘김음) |
| `correct` / `wrong` | 퀴즈 판정 |
| `hit` / `damage` | 전투 |
| `fanfare_s` / `success` | 스텝 완료 / 코딩 성공 |
| `boss_down` | 보스 격파 |

> 부스는 시끄럽다. **BGM 기본 볼륨 0.35, SE 0.6.** `M`으로 음소거.
> 헤드폰이 있으면 강사가 `M`을 한 번 더 눌러 켜 준다.

---

## 12. 부스 운영 매뉴얼 (강사용)

### 12-1. 개장 전 세팅 (10분)

1. PC에서 `dist/spike-prime-rpg.html` 더블클릭 → **F11 전체화면**
2. 소리 확인 (스피커 볼륨 30% 정도)
3. 허브 전원 ON, 배터리 확인. **여분 허브 1대 충전 상태로 대기**
4. 부품 트레이 준비 — 학생 1명분:

| 칸 | 내용 |
|---|---|
| ① | 허브 1 |
| ② | 중형 모터 1 |
| ③ | 짧은 케이블 1 |
| ④ | 빔 7칸 1 |
| ⑤ | 십자 커넥터 1 |
| ⑥ | 검정 마찰핀 2 |
| ⑦ | 장식 브릭 2 · 분리 도구 1 |

> **예비 부품을 같은 칸에 넣지 않는다.** 개수 세기가 학습 과제다. 예비(빔·커넥터·핀)는 강사가 따로 들고 있는다.

### 12-2. 1인 사이클 (10분 + 정리 2분)

| 시점 | 강사 행동 |
|---|---|
| 착석 | 「방향키로 움직이고 Z로 말 걸면 돼. 시작!」 — **설명은 이 한 마디만** |
| 0~2분 | 개입하지 않는다. 화면이 다 가르친다 |
| 2~5분 | 조립 STEP 2·3에서만 지켜본다. 핀이 안 들어가면 **누르는 힘**을 잡아준다 |
| 5~6분 | 포트 A 확인. 여기가 최다 실수 지점 |
| 6~8분 | 코딩. 화면의 주문서를 보고 **학생이 앱에서 직접 찾게** 한다. 블록 위치를 대신 눌러주지 않는다 |
| 8~10분 | 해체. 카드 순서대로 하는지만 본다 |
| 종료 | 수료증 보여주고 → `Ctrl+R` → 부품 트레이 원위치 확인 |

### 12-3. 트러블슈팅

| 증상 | 대응 |
|---|---|
| 모터가 안 움직인다 | ① 포트 A 맞나 ② 케이블 끝까지 꽂혔나 ③ 허브 가운데 버튼 눌렀나 — 이 순서로 |
| 핀이 안 빠진다 | 정상. 분리 도구 사용. 손톱 금지 |
| 화면이 작다 | F11. 브라우저 확대는 Ctrl+0으로 초기화 후 새로고침 |
| 소리가 안 난다 | 화면 아무 곳이나 클릭 (브라우저 오디오 잠금 해제) 후 `M` 확인 |
| 학생이 막혔다 | `◀ 이전`으로 앞 페이지를 다시 보여준다. 조립도가 곧 답이다 |
| 시간이 크게 밀렸다 | `F2`로 챕터 점프. **해체(MAP3)는 건너뛰지 않는다** — 정리는 반드시 시킨다 |
| 다음 학생이 바로 온다 | `Ctrl+R`. 자동으로도 90초 후 초기화된다 |

### 12-4. 놓치면 안 되는 학습 포인트 3개

부스에서 시간이 밀려도 **이 세 가지는 반드시 학생 입으로 말하게 한다.**

1. **검정 핀 = 고정, 회색 핀 = 회전** (색으로 구분)
2. **프로그램의 포트 = 실제 꽂은 포트** (안 움직이면 여기부터)
3. **해체는 조립의 역순, 케이블 먼저, 도구로** (정리까지가 실습)

> 게임이 조립을 판정하지 않으므로(§7-0), **조립의 옳고 그름은 전적으로 강사가 본다.**
> 특히 3페이지(핀 2개 고정)와 5페이지(포트 A) 두 곳만 눈으로 확인하면 나머지는 틀려도 굴러간다.

---

## 13. 검수 체크리스트 (S8 완료 기준)

### 13-1. 기능
- [ ] 타이틀 Enter → 3초 내 조작 가능
- [ ] MAP1→2→3→엔딩 풀 플레이 무오류 (마우스만 / 키보드만 **각각**)
- [ ] 가이드 6페이지 전부 표시, `◀ 이전` / `다음 ▶` 양방향 정상 (1페이지 이전 비활성)
- [ ] 조립도 6종 애니메이션 정상, 페이지 재진입 시 처음부터 재생
- [ ] 코드 가이드: 블록 4줄 색·문장이 SPIKE App과 일치, 실행 순서 루프 정상
- [ ] 보스 4문제 = 해체 4카드, 오답 시 재출제
- [ ] 도감 15종 전부 열람, 수집률 100% 도달 가능
- [ ] `Ctrl+R` / `F1` / `F2` / `F3` / `M` 모든 씬에서 동작
- [ ] 90초 무입력 → 10초 경고 → 어트랙트 + 완전 초기화
- [ ] **2회 연속 플레이 시 이전 상태가 전혀 남지 않음** (순환형 핵심)

### 13-2. 표시
- [ ] 1366×768 / 1920×1080 / 1280×1024 에서 정수배 스케일 정상
- [ ] 한글 도트 폰트 깨짐 없음 (Windows 크롬·엣지 **양쪽**)
- [ ] 텍스트 잘림 없음 — 특히 패널 설명 3줄·⚠주의·블록 라벨
- [ ] 색약 대비: 정답/오답을 **색만으로** 구분하지 않는다 (✓/✗ 기호 병기)

### 13-3. 배포
- [ ] `dist/spike-prime-rpg.html` **단일 파일**, 외부 요청 0 (DevTools Network 비어 있음)
- [ ] `file://` 더블클릭 실행 성공
- [ ] 파일 크기 2MB 이하
- [ ] `npm test` 스모크 전 씬 통과

### 13-4. 실전 리허설
- [ ] 중학생 3명 파일럿. **10분 내 완주율 2/3 이상**이면 통과
- [ ] 실패 시 조정 순서: ① 조립 스텝 문구 단순화 → ② 힌트 1단계를 더 직접적으로 → ③ 퀴즈 1문제 축소

---

## 14. 리스크와 대응

| 리스크 | 확률 | 영향 | 대응 |
|---|---|---|---|
| 10분을 넘긴다 | **높음** | 뒷사람 대기 | 타이머는 참고용이되 강사가 `F2` 보유. 조립 스텝 문구를 짧게 유지 |
| 학생이 화면만 보고 실물을 안 만진다 | 중 | 학습 실패 | 게임에 판정이 없으므로 **강사 관찰이 유일한 방어선**(§12-4). 페이지를 넘겨도 진도가 나간 게 아니라는 걸 강사가 인지할 것 |
| 핀 조립에서 물리적으로 막힌다 | 중 | 시간 폭발 | 예비 부품 + 강사 물리 개입 허용(§12-2). 여기만 유일하게 손을 대준다 |
| 학교 PC가 구형 브라우저 | 중 | 실행 불가 | ES5 문법 유지(현재 코드 준수 중), Canvas 2D만 사용 |
| 부품 분실 | 중 | 다음 회차 불가 | 해체 4단계 = **분류 정리**까지 문제로 강제. 강사가 트레이 원위치 확인 |
| 소음으로 소리가 안 들림 | 높음 | 몰입 저하 | 모든 정보를 **화면에 텍스트로도** 표시. 소리는 보조일 뿐 |
| 마우스 고장 | 낮음 | 진행 불가 | 전 UI 키보드 폴백 필수 (§7-4). 가이드 전용이라 사실상 Enter 하나면 끝난다 |

---

## 15. 확장 로드맵 (v2 이후)

| 우선도 | 항목 | 비고 |
|---|---|---|
| 높음 | **20분 확장판** | 센서 3종 실습 추가(거리센서로 멈추는 로봇). 부스가 아닌 정규 수업용 |
| 높음 | 조립 대상 2번째 「바퀴 카트」 | 라지 모터 2개 + 움직임(🔵) 블록 카테고리 해금 |
| 중 | **드래그 앤 드롭 코딩 퍼즐** | v1에서 폐기(§8-1). 20분 확장판에서 시간 여유가 생기면 부활 후보 |
| 중 | 2인 협동 모드 | 한 명은 조립, 한 명은 코딩. 화면 분할 |
| 중 | 강사용 통계 화면 | 회차별 랭크·소요시간 집계 (localStorage, 순환형 규칙의 유일한 예외) |
| 낮음 | 실물 허브 연동 | Web Bluetooth로 실제 실행 여부 자동 확인. 학교 PC 권한 문제로 후순위 |
| 낮음 | 수료증 인쇄/QR | 프린터 유무에 의존 |

---

## 16. 텍스트 리소스 — 대사 / UI JSON 분리

### 16-1. 왜 분리했나
부스 현장에서 문구를 고치는 사람은 개발자가 아니다.
학년·학교·강사에 따라 말투와 용어를 바꿔야 하는데, 그때마다 코드를 열 수는 없다.

### 16-2. 두 파일

| 파일 | 담당 | 기준 |
|---|---|---|
| `content/ui.ko.json` | 화면 텍스트 — 버튼·라벨·가이드 문구·엔딩 항목·**폰트 설정** | 화면에 붙어 있는 것 |
| `content/dialogue.ko.json` | 대사 — NPC가 말하는 것 전부 | 사람이 입으로 말하는 것 |

키는 **영어 점 표기법**. 값은 문자열 / 배열(여러 칸) / `{변수}` 치환을 지원한다.

### 16-3. 불러오는 경로 (우선순위)

| 순서 | 방법 | 쓰이는 곳 |
|---|---|---|
| 1 | `<script type="application/json">` 인라인 | **배포본** — `build.mjs` 가 구워 넣는다 |
| 2 | `fetch(data-src)` | 개발 서버 (`npm run serve`) |
| 3 | 코드의 fallback | JSON 이 통째로 없을 때의 안전망 |

> `file://` 은 `fetch` 가 CORS 로 막힌다. 그래서 **배포본은 반드시 인라인**이어야 하고,
> `build.mjs` 가 이를 자동으로 처리하며 그 과정에서 `JSON.parse` 로 문법을 검사한다.

### 16-4. 재빌드 없는 현장 수정
- 게임 화면에 **JSON 파일 드래그&드롭** → 즉시 교체
- **F4** → 파일 선택
- `FileReader` 를 쓰므로 `file://` 에서도 동작한다. 새로고침하면 원상복구.

### 16-5. 키 누락 방어
`S.Text` 는 조회됐지만 JSON 에 없던 키를 `missing` 에 쌓는다.
스모크 테스트가 전 씬을 돌린 뒤 이 목록이 비어 있는지 검사하므로,
키를 잘못 지우면 `npm test` 가 **어떤 키인지 이름까지 찍어서** 실패한다.

### 16-6. 폰트 — Mona 비트맵 폰트 (해결됨)

시스템 폰트를 알파 이진화하던 방식은 10px 이하에서 글자가 깨졌다.
실측: `0.3초` → `D.5초`, `SPIKE` → `8PIKE`. 주문서(§8)가 학생에게 **틀린 숫자**를
보여주는 심각한 문제였다.

**Mona 비트맵 폰트 도입으로 해결.** 세 가지를 바꿨다.

| 항목 | 내용 |
|---|---|
| 이진화 | **끈다** (`binarize: false`). 진짜 픽셀 폰트에는 오히려 획을 깨뜨린다 |
| 크기 사다리 | 비트맵 폰트는 설계 크기와 정수배에서만 선명하다. 요청 크기를 스냅한다 |
| 서브셋 | 원본 TTF 30MB → 한글 전체 포함 woff2 **228KB**. `build.mjs` 가 base64 로 인라인 |

사다리: 9~11 → Mona10@10 / 12~17 → Mona12@12 / 18~25 → Mona10@20 /
26~33 → Mona12@24 / 34~45 → Mona10@40 / 46~ → Mona12@48.
`ui.ko.json` 의 `font.ladder` 로 교체 가능하다.

서브셋 범위를 "쓰는 글자"가 아니라 **한글 음절 전체**로 잡은 이유:
JSON 을 고쳐 새 문구를 넣는 것이 §16 의 기능인데, 쓰는 글자만 넣으면
문구를 고치는 순간 두부(□)가 뜬다. 20KB 아끼자고 기능을 깨뜨릴 수 없다.

> 배포본 크기 181KB → **485KB** (목표 2MB 이내).
> ⚠ 폰트가 HTML 에 임베드되므로 학교 배포 전 Mona 폰트의 재배포 조건을 확인할 것.
> 비교 도구: `scripts/fonttest.html` (폰트 × 크기 × 이진화 매트릭스).

두 가지 함정이 있었고 둘 다 실측으로 잡았다.

1. **웹폰트 로드 경쟁.** `ctx.font` 는 웹폰트 로드를 유발하지 않는다. 그냥 띄우면
   첫 화면이 fallback 으로 그려지고 그 결과가 글자 캐시에 영구히 박힌다.
   `S.Font.preload()` 로 명시적으로 로드하고 끝나면 캐시를 비운다 (3초 타임아웃).
2. **cmap 에 있어도 깨지는 글리프.** `▶ ◀` 는 Mona 에서 통짜 사각형으로 그려진다.
   `→ ←` 로 교체했다. 목록은 `src/font/README.md`.
---

## 17. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.0 | — | 전 항목 확정. S1~S4·S7 구현 완료 |
| **v1.3** | 2026-09-21 | **S5·S6 구현 완료** — `steps.js`·`blocks.js`·`mission.js`·`missionPanel.js` 추가, `field.js` 배선. **§16 텍스트 리소스 신설** — 대사/UI JSON 분리, 드래그&드롭·F4 런타임 교체, 빌드 인라인, 키 누락 스모크 검사. 폰트 `binarize` 훅 추가 및 소형 글자 깨짐 문제 기록 |
| v1.2 | 2026-09-21 | **조립·코딩을 가이드 표시 전용으로 전환.** 체크리스트 판정·드래그 앤 드롭 퍼즐 폐기 → §7 전면 재작성(가이드 뷰어 6페이지), §8 전면 재작성(주문서 카드 + 앱 대응표). `ui/dragBoard.js` 폐기로 신규 파일 5 → 4, DOM 오버레이 불필요(§5). §0·§1-2·§3·§4·§12·§13·§14·§15 동기화 |
| v1.1 | 2026-09-21 | §5 해상도 **320×240 → 384×288**(실제 구현 기준)·디렉터리 실측 반영 / §3 목표 코드 "3블록" → **4블록·3카테고리** 명확화 / **§7 실습 상세(S5)**, **§8 코딩 퍼즐 상세(S6)**, **§9 보스 연출**, **§10 엔딩·랭크**, **§11 사운드**, **§12 운영 매뉴얼**, **§13 검수**, **§14 리스크**, **§15 로드맵** 신규 작성 |

> **다음 작업**: §7-7 / §8-7 의 **3개 파일** 추가 →
> `data/steps.js` (가이드 6페이지) · `data/blocks.js` (주문서 데이터) · `scenes/mission.js` (페이지 진행) · `ui/missionPanel.js` (패널 + 조립도 렌더)
> (+ `index.dev.html`·`build.mjs` 로드 순서 등록)
>
> 작업량의 대부분은 **`DIAGRAMS` 조립도 6종을 도트로 그리는 일**이다. 로직은 거의 없다.
